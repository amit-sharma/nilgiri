# Project-level ansible config: resolves our custom roles under ansible/roles/
# from the repo root. GOAD uses its own ansible.cfg under vendor/GOAD/ansible/.
[defaults]
roles_path = ansible/roles
inventory  = ansible/inventory/hosts.yml
host_key_checking = False
stdout_callback = default
